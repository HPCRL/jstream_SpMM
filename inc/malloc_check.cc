if((B == NULL) || (C== NULL) || (A_value == NULL) || (row_number == NULL) || (column_number == NULL) || (column_index == NULL) || (segment == NULL) || (vout_gold == NULL))
{
	printf("Malloc failed exiting...\n");
	exit(1);
}