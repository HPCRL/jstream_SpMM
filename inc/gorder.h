#ifndef GORDER_H
#define GORDER_H

#include <stdlib.h>     /* srand, rand */
#include <time.h>
#include <stdio.h>

int gorder(int* rowPtr, int* colInd,  int window, int n, int m, int* result);

#endif